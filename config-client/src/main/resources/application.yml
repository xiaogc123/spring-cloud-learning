server:
  port: 8881

spring:
  application:
    name: config-client

  cloud:
    config:
#      指明配置服务中心的网址。
      uri: http://localhost:8888/
#  dev开发环境配置文件
      profile: dev
#      指明远程仓库的分支
      label: master